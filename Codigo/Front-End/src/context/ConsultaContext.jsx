import { createContext } from "react";

/**
 * Este es el contexto que se crea para llamar al provider {@link ConsultasProvider} en el componente {@link Consultas}
 * @memberof Consultas
 */
export const ConsultaContext = createContext();
