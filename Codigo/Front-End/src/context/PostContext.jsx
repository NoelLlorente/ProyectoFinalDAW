import { createContext } from "react";

/**
 * Este es el contexto que se crea para llamar al provider {@link PostProvider} en el componente {@link Blog}
 * @memberof Blog
 */
export const PostContext = createContext();
